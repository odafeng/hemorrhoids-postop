# 開發維運日誌（Dev & Ops Log）

痔瘡術後 AI 衛教研究 PWA 的變更紀錄。收案期間的每次修復/部署/正式資料庫變更都記於此，
供研究可重現性與稽核參考。最新在上。

> 慣例：每筆記錄「日期 · 類型（bug / feature / db / ops）· 摘要」，並說明**為什麼**與
> **對正式環境的影響**。commit / migration 檔為權威來源，本日誌是人可讀的索引。

---

## 2026-08-02

### bug · 審核佇列可能被 200 筆上限悄悄截斷
- **症狀（潛在，尚未發生）**：`getAllChatsForResearcher()` 取 `limit(200)` 且僅依 `created_at`
  倒序。一旦 `ai_chat_logs` 超過 200 列，**最舊的會被擠出視窗**——而那正是等待審核最久的那些。
  以 50 位病人 × 30 天的目標收案量，這是會踩到的。
- **為什麼要緊**：AI 回覆審核是本研究對「AI 衛教品質」的安全網。被截斷代表某則不當回覆
  可能永遠不會出現在待審清單裡，而畫面上完全沒有任何截斷提示。
- **修法**：查詢加一層排序 `.order('reviewed', { ascending: true })`（false 排在 true 前），
  未審核的一律優先進入 200 筆視窗，上限只會裁掉**已審核**的歷史。未動 `limit`。
- **測試**：新增 `src/utils/__tests__/chatReviewQueue.test.js`。用一個**真的會執行 order/limit
  語意**的假 query builder，斷言「回傳結果裡有沒有漏掉未審核紀錄」，而非斷言「有沒有呼叫
  .order()」——後者只是鏡射實作。修前紅燈：3 筆未審核全被擠出。
- **殘留限制（已知，未處理）**：若未審核數本身超過 200，仍會截斷，且頁面「待審核 N 則」
  會低報。但那個數字代表審核流程已停擺數週，屬流程問題而非顯示問題。
- **影響**：純前端查詢調整，經 Vercel 部署。

### ops + bug · 「有 API 費用但 AI 審核頁空的」——探針花費與病人用量分不開
- **症狀**：8/1、8/2 Anthropic 有費用產生，但研究者的 AI 回覆審核頁沒有任何待審訊息。
- **調查**：三張表交叉比對，結論一致——`ai_chat_logs` 全表僅 1 筆（07-23 TEST-001，已審核）、
  `ai_request_logs` 全表 12 筆全在 07-23、`audit_trail` 近 7 天零筆 `ai.chat_request`。
  再查 Supabase Edge Function 日誌（近 6 小時）：`health` 234 次、`ai-chat` **0 次**。
  即：**自 07-23 起無任何病人使用 AI 衛教**，審核頁是空的沒錯。全 repo 只有兩處呼叫
  `api.anthropic.com`（`ai-chat/index.ts`、`health/checks.ts`），故費用全數來自 health canary。
- **根因（非故障）**：canary 依設計就是真的送一次 `max_tokens:1` 生成——這正是抓「額度耗盡」
  的唯一方法。但它與 ai-chat 共用 `CLAUDE_API_KEY`，於是 console 上探針花費與病人使用花費
  **無法區分**，任何非零金額都看起來像有人在用臨床介入。約 940 次/天 ≈ US$0.02/天。
- **處置（commit `a675bc3`）**：
  1. **拆金鑰**：canary 改計費到 `HEALTH_CLAUDE_API_KEY`，未設定時 fallback 回
     `CLAUDE_API_KEY`（故未設 secret 前行為完全不變）。額度耗盡是**組織層級**，同組織同
     workspace 的第二把金鑰仍抓得到；拆開唯一失去的是「chat 金鑰被撤銷」的偵測，
     故**新增 `chat_key` 致命檢查**——用 `CLAUDE_API_KEY` 打 `GET /v1/models`，需認證但
     **零 token 費用**，把覆蓋率補回來。
  2. **補 ai-chat 盲點**：把 SSE relay 抽成 `stream.ts`（可注入、可測），並在串流中途拋錯時
     補寫 `ai_request_logs`。原本 Anthropic 已回 200、已生成、已計費，但串流斷掉那條路徑
     **三張表都不寫**——重複發生也完全看不見。同時把 metrics/audit 改為 `close()` 前
     `await`：edge isolate 可能在回應完成瞬間被回收，fire-and-forget 的 insert 會被丟掉。
  3. **審核頁標籤**：`ChatReview` 標題原寫「PAST 24H」，但查詢無時間過濾（僅 limit 200），
     等於告訴審核者相反的事。改為「最近 200 則」。
  4. **CI 跑 Deno 測試**：vitest 刻意排除 `supabase/**`（見 07-26 條目），導致 Edge Function
     測試**從來只在本機跑過**——包含守著 ai-chat 這個臨床介入的那些。新增 `edge-functions`
     job，並讓 `deploy-supabase` 依賴它。
- **驗證**：17 Deno + 390 vitest 全綠、lint 乾淨、build 通過。缺陷測試皆先看到紅燈才修
  （串流中斷案例修前 `metrics.length === 0`）。
- **影響**：前端有變更（審核頁標籤），經 Vercel 部署；Edge Functions 經 ci.yml 重新部署。
  病人端會看到一次「系統已更新」橫幅。**`HEALTH_CLAUDE_API_KEY` 尚待 PI 於 Anthropic Console
  建立並設定**——在那之前 fallback 生效，監控行為與先前完全相同。
- **待辦（需 PI 於 Better Stack 後台操作）**：探測間隔由實測的 ~90 秒改為 **5 分鐘**。設計文件
  原本就寫「pilot 可接受約 5 分鐘」，改後花費降至 1/3，以目前收案量偵測速度無實質損失。
- **教訓**：可觀測性本身會消費資源，而**共用金鑰會讓監控成本偽裝成使用量**。要回答「病人到底
  用了多少 AI」，權威來源是 `ai_request_logs`（每筆有 input/output tokens），不是 Anthropic
  console。另外：`ai_request_logs` 這種「打 API 的每條路徑都寫一列」的設計，讓「有沒有花錢」
  與「有沒有留訊息」成為兩個可獨立驗證的事實，本次不用猜就排除了「有聊天但寫入失敗」。
- **順帶記錄（研究面）**：HSF-001 已 POD 9、交了 8 份症狀日誌，AI 衛教使用次數為 **0**。
  這不是 bug，是真實 engagement 數據。

---

## 2026-07-26

### feature · 研究者可查看每位病人的逐日回報明細與疼痛趨勢
- **需求**：研究者畫面原本只有匯總（cohort 每列僅平均疼痛 + 遵從率），無法看單一病人
  逐日回報，也看不出疼痛趨勢。
- **做法**：概覽 cohort 每列可點 → 進該病人詳情頁（新增 `/lookup/:studyId` 路由自動載入）；
  詳情頁上方新增疼痛 NRS 趨勢圖（7D/14D/ALL），下方新增逐日症狀 timeline（唯讀）。
  資料沿用既有 `getAllReports()`，**未改動後端**；病人端 `History.jsx` 未更動。
- **流程**：brainstorm → spec → plan → 4 個 TDD 任務（每任務獨立審查）→ 全分支審查（clean）。
- **驗證**：388 筆前端測試全綠、`vite build` 通過。
- **部署**：純前端，經 Vercel（push `main` 觸發）於本日部署上線；病人端會顯示一次
  「系統已更新」橫幅（service worker 不強制重整）。
- **紀錄**：spec `docs/superpowers/specs/2026-07-26-researcher-patient-daily-detail-design.md`、
  plan `docs/superpowers/plans/2026-07-26-researcher-patient-daily-detail.md`；
  commit `bad249f`→`bc95283`，合併 `886cf89`。
- **已知取捨（deferred，非阻礙）**：研究者頁趨勢圖與 `History.jsx` 的圖為刻意重複（避免
  收案期間改動病人面向程式）；`onKeyDown` Space 未做舊瀏覽器 `'Spacebar'` fallback。

### bug + db · 研究者儀表板警示「確認」按鈕沒反應
- **症狀**：研究者/PI 按警示「確認」完全沒反應。
- **根因**：正式資料庫 `alerts` 表缺 `acknowledged_by` 欄位——補該欄位的 migration
  `20260326070000_alert_acknowledge_cols.sql` 因 migration 版本錯位（靠手動套用）**從未套進
  production**；前端 `acknowledgeAlert()` 寫入該欄位時 Postgres 回 `42703`，被前端 catch
  吞掉只留 `console.error`，畫面零回饋。
- **修法（正式資料庫變更）**：經 Management API 手動對 production：
  1. `ALTER TABLE alerts ADD COLUMN IF NOT EXISTS acknowledged_by TEXT;`（純新增欄位）
  2. 新增 `researcher_ack_alerts` UPDATE policy——原本 `alerts` 僅 `pi_manage_alerts`（限 pi）
     可寫，故新增讓 researcher 也能確認**自己主刀醫師病人**的警示（沿用現有 surgeon 範圍模型）。
- **影響**：**不需前端部署**；補完後 PI/researcher 於 App 內按「確認」即正常生效並寫入
  `audit_trail`。以 `id=-1` 空更新驗證 `42703` 已消除、未動任何真實資料。
- **附註**：本次亦確認 **production RLS 與磁碟 `base_schema` 不一致**（prod 的
  `researcher_read_alerts` 為 surgeon 綁定）——正式環境 RLS 須以 `pg_policies` 直接查詢為準，
  勿以磁碟 migration 為權威。
- **紀錄**：migration `prototype/supabase/migrations/20260726000000_researcher_ack_alerts.sql`；
  commit `84ea236`。

### ops · ai-chat uptime 主動監控（health endpoint + Healthchecks.io）
- **需求**：ai-chat 是臨床介入，收案期間不能默默斷線。最陰險的失效是 Anthropic 用量上限——
  首頁照樣 200、聊天卻 400/500，ping 首頁抓不到。既有 health 函式只驗 `CLAUDE_API_KEY`
  有沒有設，驗不到「通不通 / 有沒有額度」。
- **做法**：health 函式改為**主動 canary**——用與 ai-chat 同一把 `CLAUDE_API_KEY` 打
  `max_tokens:1` 真生成（撞得到用量上限）＋ `rag_documents` count（不回傳 PII）＋ OpenAI
  `/v1/models`（非致命）。邏輯以依賴注入寫在 `checks.ts`、單元測試 10/10；`index.ts` 為薄殼。
  加 `HEALTH_TOKEN` query gate（未帶 token 回 401，擋未授權觸發付費呼叫）。
  `.github/workflows/uptime.yml` 由每 6h 升級為 `*/5` 探測＋重試，成敗回報 Healthchecks.io
  （dead man's switch + 手機告警）。
- **影響（正式環境）**：`supabase functions deploy health --no-verify-jwt` 部署到 prod
  （**純附加、不碰 ai-chat 介入、無前端變更 → 不需 banner**）；設 Supabase secret
  `HEALTH_TOKEN`、GitHub secrets `HEALTH_TOKEN` / `HC_PING_URL`。
- **驗證**：prod 冒煙 no-token→401、with-token→200 healthy（修復後）；本地 `deno test` 10/10。
- **紀錄**：spec `docs/superpowers/specs/2026-07-26-ai-chat-uptime-monitoring-design.md`、
  runbook `prototype/supabase/functions/health/README.md`；commit `ccc7eaf`→`06dd74c`。
- **剩餘手動**：Healthchecks.io 後台把 Integrations 接到 ntfy。

### incident · Anthropic 用量上限導致 ai-chat 對病人停機
- **發現**：上述監控**上線第一輪冒煙測試即抓到**——Anthropic 回 `HTTP 400`：
  「You have reached your specified API usage limits. You will regain access on
  2026-08-01 00:00 UTC.」
- **根因**：Anthropic 帳號達到**自訂的每月用量/花費上限**；ai-chat 與 canary 共用同一把 key，
  故病人端 AI 問答同步失效——典型「網站活著、聊天壞掉」的靜默停機。
- **影響**：AI 問答中斷（靜態衛教內容仍在 PWA，不完全開天窗）。若不處理將持續到
  8/1 00:00 UTC 自動重置。
- **處置**：PI 於 Anthropic Console 調高每月上限 → **立即恢復**；health endpoint 下一輪
  自動翻回 200 healthy（已實測確認）。
- **可重演風險**：上限每月重置，收案量若再撞上會再次靜默停機——監控會再告警，但根治須把
  上限設在高於單月收案用量。

### ops · uptime 探針改用 Better Stack（退役 GitHub Actions cron）
- **症狀**：Healthchecks.io 的 check 一直 UP/DOWN flapping，但 ai-chat 實測 healthy。
- **根因**：workflow 設 `*/5`，但 GitHub 對高頻 scheduled workflow 大幅節流，**實際約每
  29 分才跑一次**（12:11→12:40→13:10）；HC 卻設「每 5 分該收到心跳」（period 5m + grace
  6–8m），中間 ~24 分收不到 ping → dead man's switch 判 DOWN → 下次 run 又 UP。屬監控設定
  假象，非服務跳動。（唯一真 DOWN 是 12:40 撞到 Anthropic 用量上限那次。）
- **處置**：退役 `.github/workflows/uptime.yml`、移除其 GitHub secrets（`HEALTH_TOKEN`、
  `HC_PING_URL`）；改用 **Better Stack 主動探針**直接打 token'd `health` endpoint。health
  函式與 `HEALTH_TOKEN` 不變。
- **影響**：無前端、無 ai-chat 變更；commit `d21a8b8`。Better Stack 帳號/monitor 由 PI 設定
  （指向 health URL、非 2xx 即 DOWN、告警接手機）；HC check 應暫停/刪除以免持續假 DOWN。
- **教訓**：GitHub scheduled workflow 是 best-effort，不適合做緊間隔 liveness 探測。

### bug + ops · CI 連鎖失敗（vitest 撈 Deno 測試 → deploy smoke test 撞 token gate）
- **症狀**：push 後 `CI — Lint, Build & Tests` red。
- **第一層**：CI 的 `vitest run` 用預設 glob 撈到 `supabase/functions/health/checks.test.ts`
  （Deno 測試），Node 無 `Deno` global → suite 掛。修：`vitest.config.js` 的 exclude
  加 `supabase/**`（edge function Deno 測試不歸 vitest 管，走 `deno test`）。commit `bf65dd1`。
- **第二層**（第一層修好後才浮現）：`deploy-supabase` job（`needs:[test,e2e]`，原被擋住）
  解鎖後跑起來，其 health smoke test 打 endpoint **沒帶 token**，撞到新加的 `HEALTH_TOKEN`
  gate → 401 → `curl -f` exit 22。修：CI 帶 token。commit `1ba3cff`。
- **安全審查 finding**：token 放 URL query 會進請求 log → 改成 **`x-health-token` header**
  （保留 `?token=` 相容，Better Stack 不受影響）；health 重新部署 prod 支援 header。
  commit `5b4e43d`。
- **影響**：CI 全綠（實測 watch exit 0，含兩支 smoke test）。`HEALTH_TOKEN` 現有三個消費端
  （Supabase secret／GitHub secret／Better Stack），輪替時三處同步。
- **附註**：green main push 會觸發 `deploy-supabase` 重新部署**所有** edge function（含 ai-chat，
  來源同 prod、行為無變化）；本次真正變動的只有 health。
