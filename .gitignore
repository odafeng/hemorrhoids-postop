# OS files
.DS_Store
Thumbs.db

# IRB documents — confidential, do not commit
IRB/

# Vercel local config
.vercel/

# Environment variables (API keys, secrets)
.env
.env.local
.env.*.local
prototype/.env
prototype/.env.local

# R session files
.Rhistory
.RData
.Rproj.user/

# Personal documents
CV_*.docx
STATUS

# Supabase data dumps taken before destructive cleanups — contains patient
# records and auth users, must never be committed.
backup_before_cleanup_*/

# Local browser-automation artifacts
.playwright-mcp/
*.webp
login-prod.png

# 收案個資 — 絕不進版本控制（repo 為 public）
# 對照表含姓名與病歷號；同意書含簽名。兩者都是 IRB 個資文件。
收案文件/收案對照表*.xlsx
收案文件/*同意書*.pdf
收案文件/*受試者*.pdf
