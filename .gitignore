# OS files
.DS_Store
Thumbs.db

# IRB documents — confidential, do not commit
IRB/

# Vercel local config
.vercel/

# Environment variables (API keys, secrets)
.env
.env.local
.env.*.local
prototype/.env
prototype/.env.local

# R session files
.Rhistory
.RData
.Rproj.user/

# Personal documents
CV_*.docx
STATUS

# Supabase data dumps taken before destructive cleanups — contains patient
# records and auth users, must never be committed.
backup_before_cleanup_*/

# Local browser-automation artifacts
.playwright-mcp/
*.webp
login-prod.png
