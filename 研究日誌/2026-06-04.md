# 2026-06-04（三）— 收案準備 Day 1

## 今日目標

正式收案前的系統總檢查與文件準備。

---

## 完成事項

### 1. 收案流程全面審查

- 從研究人員角度完整走過 6 步收案流程：
  產生邀請碼 → 協助病人註冊 → 簽署同意書 → 填寫手術紀錄 → 確認收案 → 每日回報
- 確認 App 前端（Login → ConsentPage → Dashboard → SymptomReport）與後端（Supabase Edge Functions）的資料流完整無斷點

### 2. 收案文件製作

建立 `收案文件/` 資料夾，包含 4 份可列印文件：

| 文件 | 用途 |
|------|------|
| 收案流程指引.pdf | 研究人員 step-by-step 操作手冊（A4 橫式） |
| 收案對照表\_模板.xlsx | Study ID ↔ 姓名/病歷號對照表（50 列，下拉驗證） |
| 領據\_車馬費\_300 元.pdf | NT$300 補償費領據（兩聯式） |
| App\_QR\_Code.pdf | App 安裝 QR Code 海報（A4 直式） |

### 3. 專案資料夾整理

- 開發用截圖/影片移至 `開發素材/`
- CV 移入 `IRB/`
- HTML 原始檔與生成腳本放入 `收案文件/_原始檔/`

### 4. 程式碼品質修復

**ESLint：46 errors + 7 warnings → 0 errors, 0 warnings**

主要修復：
- 移除 `supabaseService.js` 中 `saveReport()` 的 debug `console.log`
- 修正 `set-state-in-effect`：改用 `useState(initializer)` 取代 effect 內同步 setState
- 修正 `static-components`：DebugPanel 內部 `<Row>` component 改為 `renderRow()` 函式
- 移除未使用變數/import（`getPatient`, `isProduction`, `e2`, `patients` 等）
- 修正 `NotificationSetup` 變數宣告順序問題
- 測試檔補上正確的 vitest globals import
- 所有 318 個單元測試通過

### 5. 安全性強化

#### 5a. 隱藏 Demo 模式按鈕
- Login 頁面的「Demo 模式（無需登入）」按鈕改為只在 `import.meta.env.DEV` 時顯示
- 生產環境（Vercel）不會渲染，避免病人誤觸

#### 5b. 移除全域邀請碼 fallback
- `patient-onboard` Edge Function 原有 two-tier 驗證：per-patient token + 全域 `HEMORRHOID2026`
- 移除 Tier 2 全域 fallback，現在 **所有新病人必須持有研究人員產生的 per-patient invite token**
- Edge Function 已重新部署至 Supabase（`supabase functions deploy patient-onboard`）

#### 5c. 清除 .env.test 明文密碼
- 移除硬寫在 `.env.test` 中的測試帳號密碼、service role key
- 改為空白佔位，需從環境變數或 CI secrets 注入
- README 更新：移除 `GLOBAL_INVITE_TOKEN` 相關說明

### 6. Supabase 資料庫清理

刪除所有測試資料，回到乾淨狀態：

| 項目 | 操作 |
|------|------|
| 測試病人帳號 | 刪除 `odafeng7@gmail.com`（TEST-002）、`c0925806571@gmail.com`（HSF-002） |
| 多餘 PI 帳號 | 刪除 `drfredrichuang@gmail.com`（RESEARCHER-002） |
| 測試資料 | 清除 patients(2)、symptom_reports(4)、study_invites(3)、ai_chat_logs(1)、surgical_records(1)、pending_notifications(60)、audit_trail(111) 等 |
| 保留 | PI 帳號 `odafeng@hotmail.com` + rag_documents(106 筆衛教知識庫) |

### 7. 整合測試

新增 `scripts/integration-check.mjs`，32 項測試全數通過：

- Database 連線（Anon + Admin）
- RLS 權限隔離（匿名無法讀寫病人資料）
- 13 張表 schema 驗證
- RAG 知識庫完整性（106 筆 + embeddings）
- Auth 系統（PI 帳號存在、無殘留測試帳號）
- Edge Functions（health OK、onboard/ai-chat 正確擋未認證請求）
- 乾淨狀態確認（所有業務表為空）

---

## Git Commits

| Hash | Message |
|------|---------|
| `431ff07` | docs(enrollment): add enrollment toolkit and reorganize project root |
| `67f70ed` | fix(lint): resolve all ESLint errors and warnings (46 errors → 0) |
| `e4052f5` | fix(login): hide Demo mode button in production |
| `6f3daa6` | security(onboard): remove global invite token fallback |
| `0e989b6` | security(env): remove hardcoded credentials from .env.test and update docs |
| `e3985bf` | test(integration): add Supabase connectivity smoke test |

---

## 目前系統狀態

- Supabase：**ACTIVE_HEALTHY**（ap-south-1, Postgres 17.6）
- ESLint：**0 errors, 0 warnings**
- Unit tests：**318 passed**
- Integration tests：**32 passed**
- Auth：1 個 PI 帳號
- 業務表：全部清空，ready for enrollment

## 明日待辦

- [ ] 部署最新前端到 Vercel（含 Demo 按鈕隱藏）
- [ ] 第一位病人收案演練（dry run）
- [ ] 確認 push notification 在 iOS/Android 的表現
