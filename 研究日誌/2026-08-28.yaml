date: 2026-08-28
weekday: 五
phase: 收案執行
day: 37        # 自 2026-07-23 首例收案起算
objective: >
  起點是 PI 的一句盤點：「對照表還有哪些人的 PII 還沒對齊？」問題本身預設缺口在
  對照表裡面，查完發現方向相反——對照表上的八列全部對齊，缺的是兩位連對照表都
  還沒登錄的新收案。補登過程另外撞出 Excel 樣式的一個 parity 陷阱，
  以及 CLAUDE.md 現行寫法涵蓋不到的一段。
  同一天往下延伸成兩件事：一是修掉 dashboard.py 讀死欄位的啟用率，二是把
  「滿 30 天結案」做成 App 端的完整機制，分五層以 stacked PR 送出並全部上線。
  日終再收一位新個案 HSF-006，這次在當天就補齊了兩份紀錄。

# 註：本檔位於公開 repo，只記錄機制層級資訊。
# 受試者姓名、病歷號、聯絡方式與加密金鑰一律不寫入，僅以 Study ID 指涉。

trigger:
  summary: >
    PI 詢問收案對照表中哪些受試者的 PII 尚未與 pii_patients 對齊。
  event_time: 2026-08-27T17:15:00Z   # 台灣時間 2026-08-28 01:15

findings:
  - id: 1
    severity: medium
    issue: 缺口不在對照表內部，而是兩位個案完全沒進入任何一份人工紀錄
    detail: >
      收案對照表第 6–13 列共八筆（HSF-001~005、WCC-001/002、FIH-001），
      逐筆在 pii_patients 都有對應列，四個加密欄位的密文長度反推明文位元組數
      與對照表欄位內容一致。缺的是 FIH-002 與 FIH-003：App 註冊流程已自行建立
      patients 列（created_at 分別為 2026-08-26T11:28Z、2026-08-27T14:08Z），
      但對照表停在序號 8，pii_patients 也只有八筆。
      pii_patients 無孤兒列（沒有 patients 對不到的 study_id）。
    consequence: >
      FIH-002 延遲 2 天、FIH-003 延遲 1 天。兩者皆已進入追蹤期，
      若期間需要聯絡受試者，兩份能還原身分的紀錄都查不到人。
      這正是 CLAUDE.md「兩者不會互相提醒，也不會有人報錯」描述的模式，
      只是這次連對照表那一端也沒有被打開過——2026-08-12 的
      lesson_zone_a 說「真正在運作的是那份 Excel 對照表，因為每收一位就得打開它一次」，
      這個前提在收案者不是 PI 本人時不成立。三位缺口個案全部是 FIH 收的。

  - id: 2
    severity: low
    issue: 空白列 14–55 是隔列上色，樣式複製必須涵蓋整列 A–N
    detail: >
      待填區奇數列（15、17…55）填色 FFF2F7FB，偶數列（14、16…54）無填色，
      兩者在全部 14 欄的 style id 都不同。已填資料列 10–13 樣式完全一致且無填色，
      表示一列從「待填」轉為「資料」時是整列換掉 banding。
      因此新增列必須對 A–N 全欄複製前一資料列的 _style，
      只複製有值的欄位會讓 J/K/L/N 等空欄殘留 banding。
    consequence: >
      CLAUDE.md 現行寫法「設完 .value 要再 copy() 前一列的 _style」方向正確，
      但沒有提到 parity，也容易被讀成「只需處理有寫值的欄位」。
      本次第 15 列若只複製有值欄位，會在 J/K/L/N 留下藍底。

  - id: 3
    severity: low
    issue: pii_patients.mrn_enc 是 NOT NULL，phone_enc 才可為空
    detail: >
      pii_patients 僅 phone_enc 與 national_id_enc 允許 NULL。
      WCC-002 的 phone_enc 為 NULL 與對照表 H 欄「未取得」一致，不是漏填。
      病歷號缺漏則會在 INSERT 當下被 schema 擋下，不會靜默寫入半筆。
    consequence: >
      補登流程若缺病歷號，是硬阻斷而非需要事後對帳的軟缺口。這一欄不必列入核對清單。

  - id: 4
    severity: info
    issue: 本機無 openpyxl，系統 Python 為 PEP 668 externally-managed
    detail: >
      python3.14（homebrew）與 python3.11 皆無 openpyxl，
      `pip install` 被 externally-managed-environment 擋下。
      改在 scratchpad 建 venv 安裝 openpyxl 3.1.5，不污染使用者環境。
    consequence: >
      對照表的讀取可用 stdlib zipfile + ElementTree 完成（本次盤點階段即如此），
      但寫入要保留樣式與資料驗證，實務上仍需 openpyxl。
      下次維護對照表前先確認這個 venv 是否還在（scratchpad 會隨 session 清掉）。

  - id: 5
    severity: medium
    issue: 受試者識別資料進入了 Claude Code 的 transcript 與 claude-mem observation DB
    detail: >
      本次補登所需的姓名、出生日期、聯絡電話、病歷號由 PI 直接貼入對話，
      而非放在本機暫存檔後讀取即刪（2026-08-14 用的做法）。
      落點為 ~/.claude/projects/ 下的 transcript，以及 claude-mem 的 observation 資料庫。
      兩者皆在本機、不在 repo，未觸及公開 repo 的硬性限制。
    consequence: >
      多出一份非預期的留存副本，且 claude-mem 的內容會被後續 session 讀回。
      待決：是否清理本次 transcript 與對應 observation。

  - id: 6
    severity: info
    issue: patients.app_activated 的處置尚未收斂
    detail: >
      PI 提出「要嘛拿掉，要嘛預設都 True」。現況為 NOT NULL DEFAULT FALSE，
      codebase 無任何寫入處，十筆全為 false。
      讀取端只有 prototype/scripts/dashboard.py:174 與 :374（本機維運腳本，非 production）。
      2026-08-12 已記錄「PI 已決定不使用，啟用率改以首次回報計」，
      但 dashboard.py 至今仍讀這個欄位。
    consequence: >
      改 DEFAULT 為 TRUE 會把「沒有資料」偽裝成「是」，方向與 lesson_zone_a
      指出的問題相同，只是把假陰性換成假陽性；DROP COLUMN 屬收案期間的
      production schema 變更。兩者都不必動就能解決讀取端。
      已改讀取端（見 D5）：舊定義啟用率 0%（十筆全 false），
      新定義 90%（9/10）。唯一未啟用者為 FIH-003，其手術日即為今日，
      尚未到第一次回報。

  - id: 7
    severity: high
    issue: 追蹤期結束在系統的任何一端都不會出聲，HSF-001 已逾期五天仍為 active
    detail: >
      fn_report_days() 回傳 [0,1,2,3,4,5,6,7,9,11,13,20,27]——POD 30 不是回報日，
      最後一個是 POD 27。病人端在 POD 30 當天不會有推播也不會有卡片；
      每日摘要腳本的主迴圈又在 pod > FOLLOWUP_DAYS 直接 continue，
      逾期者從畫面上消失。HSF-001 手術日 2026-07-24，POD 30 為 2026-08-23，
      今日為 POD 35，study_status 仍為 active，回報 9/13。
    consequence: >
      追蹤期結束是純行政終點，沒有任何機制提示。第一位受試者已經漏過去五天。

  - id: 8
    severity: medium
    issue: study_status 只有寫入 'active' 的地方，沒有任何地方寫 completed 或 withdrawn
    detail: >
      全 codebase 對 study_status 的寫入僅 patient-onboard/index.ts:232 一處，
      值寫死為 "active"。completed_at 欄位亦從未被寫過。
      讀取端有 check-adherence（.eq("study_status","active")）、
      多個 view 的 WHERE study_status != 'withdrawn'、dashboard.py 的表格欄位。
    consequence: >
      與 app_activated 同構：宣告了 DEFAULT、有讀取端、沒有寫入端。
      直接影響本日新增的逾期清單——不手動改 study_status 的話它會單調成長。
      推播端無害：patientsDueForReminder 以 reportDays 過濾，
      最大回報日 27，逾期者不會被誤推播。

  - id: 9
    severity: medium
    issue: 分層時把 lint 修正放進發現它的那一層，使下層單獨拿出來是壞的
    detail: >
      L4（前端鏡像）新增的測試用 process.cwd() 讀檔比對，ESLint 的 browser env
      沒有定義 process，該行在 L4 就違規。但修正（改用 globalThis.process）
      是在寫 L5 時才發現並順手放進 L5 的 commit。
      結果 PR #9 單獨跑 CI 直接紅在 lint，而 PR #10 是綠的。
    consequence: >
      違反「每層必須獨立正確、獨立可 revert」。修法是把修正下推到 L4 再 rebase L5，
      並且兩層各自單獨跑完整驗證（L4 單獨 444 測試、L5 448）。
      通則：修正要跟著「引入問題的那一層」，不是「發現問題的那一層」。

  - id: 10
    severity: low
    issue: gh stack submit 建立的 PR 預設是 draft，且 stacked PR 不能用 gh pr merge
    detail: >
      四個 PR 建立後 CI 全綠，但 isDraft 皆為 true，當時沒注意。
      隔日要合併時 gh pr merge 回報「This pull request is part of a stack and must
      be merged using the asynchronous merge REST API」，需改用 gh stack merge；
      而 gh stack merge 又拒絕 draft，要先 gh pr ready。
    consequence: >
      draft 預設其實有用——它讓「先手動實測再開排程」那道關卡多一層保險。
      gh stack merge <PR> 的語意是「合併到該 PR 為止」，所以 merge 10 會含 #9、
      merge 6 不會碰 #7，正好對應計畫需要的分批合併。

  - id: 11
    severity: info
    issue: 當天新收案 HSF-006 在數小時內就被發現兩份紀錄未補
    detail: >
      08-28 17:19 註冊，20:14 因例行查詢 patients 而發現 active 由 9 變 10、
      has_pii 為 false。與 FIH-002/003 延遲 1–2 天相比縮短到數小時。
    consequence: >
      發現機制仍是「剛好查到」，不是任何自動提示。findings 1 描述的結構沒有改變，
      只是這次查詢頻率高。若無人查，缺口一樣不會有人報錯。

  - id: 12
    severity: medium
    issue: ENROLLED 卡片的標籤與數字不一致，被結案機制照出來
    detail: >
      ResearcherDashboard 的 ENROLLED 卡片主數字取 activePatients
      （study_status === 'active'），小字取 studyPatients.length（全部非 TEST）。
      HSF-001 結案後兩者首次分歧，顯示為「10 of 11 total」。
      結案上線前 study_status 只有 'active' 一種值，兩個數字永遠相等，
      標籤寫 ENROLLED 卻算「仍在追蹤」完全看不出來。
      小字的 fallback 寫 `|| 40`，與 IRB Ver 2 的 50 例目標不符；
      它只在世代為空時 render，所以過期數字長期無人發現。
    consequence: >
      放著不管，招募進度的數字會隨受試者陸續完成而下降：
      9/4 WCC-001/002 結案後變 8/11，收滿 50 例並全部追蹤完成時顯示 0。
      這是 findings 6 與 8 的下游——欄位沒有寫入端時，依賴它的顯示邏輯
      也不會被檢驗。既有的「ENROLLED 不計入 TEST-001」測試 fixture 全為 active，
      從來分辨不出這兩種讀法。

hypotheses_ruled_out:
  - hypothesis: 對照表裡有受試者的 PII 尚未寫入 pii_patients
    disproved_by: >
      對照表八列逐筆比對 pii_patients 皆有對應，且四個加密欄位的密文長度
      減 66 bytes overhead 反推的明文位元組數與對照表內容一一相符
      （姓名 9 bytes = 3 中文字；病歷號 7 或 8 bytes 與實際位數相同；
      電話 10 bytes 表示已去除破折號；出生日期 10 bytes = YYYY-MM-DD）。
      缺口全部落在對照表本身沒有的兩列。

  - hypothesis: WCC-002 的 phone_enc 為 NULL 是漏填
    disproved_by: 對照表 H 欄記載「未取得」，兩邊一致；且 phone_enc 允許 NULL。

  - hypothesis: 依 CLAUDE.md 複製「前一列」樣式即可，且只需處理有寫值的欄位
    disproved_by: >
      第 15 列（奇數，待填區有 banding）與第 13 列在全部 14 欄的 style id 都不同，
      不只 C/D/H。只複製有值欄位會讓 J/K/L/N 留下 FFF2F7FB 底色。

  - hypothesis: gh stack 不存在，stacked PR 只能用 gh pr create --base 手動疊
    disproved_by: >
      對 gh 2.86.0 而言正確，但結論錯了。升級到 2.98.0 後執行 gh stack 會直接指路
      gh extension install github/gh-stack——它是官方 extension。裝上後 init/submit/
      merge 都可用，不需要繞。

  - hypothesis: lint 與型別問題只要最終 stack 頂端是綠的就沒事
    disproved_by: >
      PR #9 單獨跑 CI 紅在 lint（'process' is not defined），而其上的 PR #10 是綠的
      ——因為修正被放在 L5。stack 中每一層的 CI 是各自跑的，頂端綠不代表下層可合。

  - hypothesis: 把 close-followup 加進 ci.yml 部署清單屬於 L3（排程層）
    disproved_by: >
      ci.yml 的部署步驟是逐一列名函式而非掃目錄。若該行留在 L3，L2 合併後
      production 根本不會有這個函式，「先手動實測再開排程」的整個關卡設計失效。
      該行 amend 進 L2。

  - hypothesis: FIH 收案的三筆 app_activated 皆為 false，可能是該收案者的啟用流程有問題
    disproved_by: >
      症狀回報筆數顯示 FIH-001 有 6 筆、FIH-002 手術隔日即回報 1 筆，
      兩人都在使用 App。false 是死欄位的假象而非啟用問題。
      FIH-003 為 0 筆，但其手術日即為今日（POD 0）。
      FIH-001 依從性偏低另有原因（2026-08-23 已查明為缺 push subscription），與此無關。

  - hypothesis: app_activated 應該改成 DEFAULT TRUE，或直接 DROP COLUMN
    disproved_by: >
      DEFAULT TRUE 會把「沒有資料」偽裝成「是」，與 2026-08-12 lesson_zone_a
      指出的問題同構，只是假陰性換成假陽性；DROP COLUMN 屬收案期間的
      production schema 變更。實際讀取端只有 dashboard.py 兩處本機腳本，
      改讀取端即可，不需要任何 DDL。

  - hypothesis: 逾期未結案的病人可能還在被 check-adherence 誤推播提醒
    disproved_by: >
      check-adherence 雖以 study_status='active' 取病人，但 patientsDueForReminder
      再以 fn_report_days() 過濾，最大回報日為 27，POD 30 之後不可能被判定為 due。
      逾期只影響本日新增的摘要清單，不影響受試者收到的推播。

  - hypothesis: 收案日期可從手術日期減一天推得
    disproved_by: >
      本次兩筆確實成立，但這是巧合而非規則。正確來源是 patients.created_at
      轉台灣時間取日期，本次兩筆分別為 2026-08-26 與 2026-08-27，與 created_at 相符。

decisions:
  - id: D1
    decision: FIH-002、FIH-003 立即補登，不等下一次收案批次
    rationale: >
      兩者已在追蹤期內，聯絡管道的還原能力不應有空窗。
  - id: D2
    decision: enrolled_at 一律取 patients.created_at，不用 now()
    rationale: >
      沿用 2026-08-14 的做法，記到的是收案時刻而非補登時刻。
      現況十筆中 HSF-001 與 FIH-001 的 enrolled_at 與 created_at 不符
      （分別差 1h49m 與 8h26m），係 Vault 流程之前補登所致，本次未回填。
  - id: D3
    decision: 本次不動 production schema，app_activated 維持原狀
    rationale: >
      收案期間的硬性限制。且讀取端只有本機腳本，改讀取端即可，不需 DDL。
  - id: D4
    decision: 對照表新增列一律整列 A–N 複製前一資料列的 _style
    rationale: 見 findings 2，parity 使得部分複製會殘留 banding。

  - id: D5
    decision: >
      dashboard.py 的啟用率改以「該 study_id 至少有一筆 symptom_report」認定，
      app_activated 欄位保留在 schema 中不動
    rationale: >
      落實 2026-08-12 已下的決議（啟用率改以首次回報計），該決議當時未同步到讀取端。
      改動 6 行、只在 compute() 收斂一次 study_id 集合供兩處讀取端共用，
      不多打 API 也不在 render 階段重掃 reports。
      欄位留作死欄位，待收案結束後隨其他 migration 一併清除。
    trade_off: >
      「有回報」量到的是受試者實際使用行為，「有 push subscription」量到的是
      通知管道是否接通，兩者不同。此處選前者，因為 primary outcome 關心的是
      回報行為本身；管道問題另由 push subscription 檢查涵蓋。
      缺點是 POD 0 當日收案者必然計為未啟用（本次的 FIH-003 即是），
      分母含當日收案者時啟用率會被低估。

  - id: D6
    decision: >
      每日 ntfy 摘要新增兩段：POD 23–30 的「⏳ POD30 倒數」與
      「🔴 已過 POD30 未結案」，後者使 priority 至少為 high
    rationale: >
      PI 要求「POD30 前一週提醒」。做成每日倒數而非單次提醒，是因為單次提醒
      當天沒看手機就完全錯過，沒有第二次機會；每日摘要本來就每天推，
      掛在既有推播上不增加通知數量。
      逾期段是 PI 明示要加的：倒數只對還沒到期的人有用，抓不到已經漏掉的
      （HSF-001 現在就在漏）。
    trade_off: >
      同一個人會連續出現八天（POD 23–30）。逾期段在 study_status 沒有寫入端的
      現況下會單調成長，需要 PI 手動結案才會消失——這使得「清單變長」本身
      成為提示，但也可能在多位受試者同時逾期時變成噪音。
      「回報 X/Y」的分母定義為「到當日為止已過去的回報日數」，
      與 v_adherence_summary 的分母不必然相同，兩者不可直接比對。

  - id: D7
    decision: 不代為將 HSF-001 的 study_status 改為 completed
    rationale: >
      結案與否是研究判斷（D28 回診是否完成、CRF 是否收齊），不是資料清理。
      逾期清單會持續提示直到 PI 自行處理。

  - id: D8
    decision: >
      App 端結案流程分五層以 stacked PR 實作：規則模組與 ADR、Edge Function、
      排程、前端鏡像、儀表板顯示。全部零 DDL
    rationale: >
      study_status 是無 CHECK constraint 的 VARCHAR(20)，寫 'completed' 不需 migration，
      這是收案期間唯一可行的路。排程走 GitHub Actions 而非 pg_cron——
      production 沒安裝 pg_cron（cron.job relation 不存在），
      既有的 check-adherence 也是這樣觸發的。
      分層依「機械性在下、需判斷在上」：規則定義 → 執行器 → 開關 → 鏡像 → UI。
      設計決策另立 docs/adr/0001-followup-close-out-at-pod30.md（repo 第一則 ADR）。

  - id: D9
    decision: Edge Function 與排程分兩層合併，中間插入一次 production 手動實測
    rationale: >
      L3 是整個 stack 裡唯一會讓 production 開始自動寫入的一層。
      分開之後 L2 合併時函式只回應帶 CRON_SECRET 的呼叫、不會自己跑，
      可以先驗證一次再開自動化。
      實測以 gh workflow run --ref followup/03-schedule 執行，
      不必合併 #7 也不必取得 secret 明文（GitHub 與 Supabase 兩端都不給讀回 secret 值，
      supabase secrets list 的 value 欄位是 SHA-256 digest）。
      當日無人到期，所以那是一次保證不寫入、但完整跑過決策路徑的實測。

  - id: D10
    decision: HSF-006 的對照表收案狀態填「已簽同意書」而非「追蹤中」
    rationale: >
      其手術日為 2026-08-29，登錄當日尚未進入追蹤期。其餘各列的「追蹤中」
      皆為術後狀態。此欄為下拉選單，PI 若要統一格式可逕行修改。

  - id: D11
    decision: ENROLLED 主數字改為累計收案，小字帶目標與追蹤中人數
    rationale: >
      對以 50 例為招募目標的研究，招募進度的數字不應該往下掉。
      已結案人數旁邊的 CLOSE-OUT 卡已有，不重複。
      `|| 40` 換成具名常數 ENROLMENT_TARGET = 50。
      本次為獨立 PR（#12），不疊在結案 stack 上——它修的是被那項工作照出來、
      而非由那項工作造成的缺陷。

verification:
  scope: >
    只驗證兩筆新列的寫入正確性與既有八筆的持續一致性，未重新驗證加密金鑰本身，
    亦未解密任何欄位回傳明文。
  checks:
    - 備份 收案對照表.bak-20260828.xlsx 已建立，git check-ignore 命中
      .gitignore:38（收案文件/收案對照表*.xlsx）
    - 第 14、15 列 B/C/D/E/F/G/H/I/M 九欄皆有值，J/K/L/N 留空（回診日期待填）
    - 第 14、15 列 A–N 全欄 style id 與第 13 列相同；字型 Noto Sans TC、置中、
      填色 00000000（banding 已移除）；E/F/I 數值格式 yyyy-mm-dd
    - 下拉清單 G6:G55 與 M6:M55 已涵蓋第 14、15 列，未調整 dv.sqref
    - INSERT 於 DO 區塊內執行，先 set_config('app.access_reason', ..., true)
      再寫入，金鑰取自 vault.decrypted_secrets 且未離開資料庫
    - 補登後全部十筆（排除 TEST-001）has_pii 皆為 true；
      新增兩筆 name 9 bytes、mrn 8 bytes、phone 10 bytes、birth_date 10 bytes，
      與來源格式相符；enrolled_at = patients.created_at 為 true
    - patients.surgery_date（2026-08-27、2026-08-28）與對照表 F 欄一致，
      consent_date 取自 patients.consent_date 轉台灣時間
    - dashboard.py 改動後 scripts/test_dashboard.py 7 項全過；
      將 compute() 還原為舊的 app_activated 邏輯後 4 項轉紅（mutation check），
      確認測試確實守得住這次改動而非恆綠
    - dashboard.py 內已無 app_activated 的功能性參照（僅剩一行註解說明原因）
    - 結案 stack 四個 PR（#6/#7/#9/#10）各自 CI 全綠後才合併，
      每層行數在 200 以內（L2 200、L3 16、L4 143、L5 +82/-6）
    - L4 與 L5 在 rebase 後各自單獨跑完整驗證：L4 lint 乾淨、444 測試、build 成功；
      L5 lint 乾淨、448 測試、build 成功
    - 前端鏡像的漂移防護以 mutation 驗證：把 src/utils/followup.js 的
      FOLLOWUP_DAYS 改成 31，六個測試轉紅
    - main 部署後以錯誤 token 探測 close-followup 與 check-adherence，
      兩者皆回 401 而非 5xx，證明函式啟動、_shared/followup.ts 有正確 bundle
    - 前端上線以字串比對確認（非 hash）：線上 bundle 含 CLOSE-OUT、結案剩、
      今天到期、逾期、已結案五組本次新增字串
    - close-followup production 實測回 {"today":"2026-08-28","closed":[],"failed":[]}
      HTTP 200；事後查 patients：completed 仍 1 筆、HSF-001 的 updated_at
      停在手動結案時刻，未被該次執行改動
    - check-adherence 於 08-28 06:34Z 的排程班次 success，
      證明 L1 把 podFor() 搬到 _shared/ 後它在 production 實際執行正常
    - HSF-006 補登後全體對帳：11 筆非 TEST，missing_pii = 0；
      新列密文長度反推 姓名 9B／病歷號 8B／電話 10B／出生日期 10B，
      enrolled_at = patients.created_at
    - 對照表第 16 列 A–N 全欄樣式與第 15 列相同，下拉清單 G6:G55 與 M6:M55 已涵蓋
    - ENROLLED 修正（PR 12）：lint 乾淨、450 測試通過、build 成功。
      兩個新測試釘住「completed／withdrawn 仍計入 ENROLLED」與「小字報出目標與
      追蹤中人數」；以 mutation 驗證——把 activePatients 放回主數字，第一個測試轉紅
    - 每日摘要新段落以四個模擬日期驗證邊界：08-26（WCC POD22，倒數段不出現）、
      08-28（WCC POD23，倒數段出現）、09-04（WCC POD30 顯示「今天到期」）、
      09-14（多人逾期時的排序與計數）。驗證時直接呼叫 build_summary()，
      不經過 push()，避免測試推播洗手機
    - 以 /usr/bin/python3（3.9.6，launchd plist 指定的直譯器）確認可執行，
      非開發機上的 homebrew 3.14
    - 端到端實跑一次含推播（SUMMARY_DATE=2026-08-28），
      keychain 取 token 與 ntfy 送達皆正常，exit 0

deliberately_not_done:
  - HSF-001 與 FIH-001 的 enrolled_at 未回填。影響收案時刻的精確度，
    不影響任何分析端點；回填會再動一次 pii_patients，留待 PI 決定
  - HSF-003/004/005 的對照表 M 欄「收案狀態」仍為空（前五列皆填「追蹤中」）。
    屬對照表欄位漏填，與 PII 對齊無關，未一併補
  - L6（手動覆寫：提前結案／退出／重開）未做。規則是無條件的，加手動入口等於把
    「要有人記得」放回流程；withdrawn 屬 IRB 層次概念，與「滿 30 天」不是同一件事
  - study_status 仍是無 CHECK constraint 的自由字串。收緊成 CHECK 或 enum 屬 DDL，
    收案期間不做
  - ci.yml 的 deploy-supabase 沒有 path filter，任何 push 到 main（含純文件）
    都會重新部署全部 9 個 Edge Function。本日發生多次。未加 paths 過濾
  - 未盤點 schema 中其餘帶 DEFAULT 的欄位。今日同型問題出現四次
    （app_activated 兩處讀取端、study_status、ENROLLED 顯示），
    逐個踩到才發現的成本已經高於一次全面盤點，但那是獨立任務
  - app_activated 欄位本身未動（未改 DEFAULT、未 DROP），只改讀取端
  - test_dashboard.py 未納入 CI。ci.yml 目前不裝 requests/numpy/matplotlib，
    為一支本機維運腳本在 CI 建 Python 環境不成比例。需手動執行：
    cd prototype && python3 -m unittest discover -s scripts -p 'test_*.py'
  - dashboard.py 的 module 層副作用（_load_key 的 SystemExit、OUTPUT_DIR.mkdir）
    未重構。測試改以 import 前設環境變數繞過，避免把改動擴散到本次範圍之外
  - 未替 HSF-001 結案（見 D7），亦未處理 study_status 沒有寫入端這件事本身。
    要在 App 端補結案流程屬收案期間的行為變更，需另行決定
  - 每日摘要腳本位於 ~/.hemorrhoids-daily/（repo 外、perms 700、不進版本控制），
    本次改動只能靠此日誌與 memory 記錄，git log 查不到
  - 未清理 transcript 與 claude-mem 中的識別資料（見 findings 5）
